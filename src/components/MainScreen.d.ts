import { Tab } from '../store/navigation/types';
interface Props {
    tab: Tab;
}
declare const _default: import("react-redux").ConnectedComponent<({ tab }: Props) => JSX.Element, Pick<Props, never>>;
export default _default;
